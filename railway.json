{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build && php artisan migrate --force && php artisan storage:link && php artisan optimize"
  },
  "cron": {
    "* * * * *": {
      "command": "php artisan schedule:run"
    }
  }
}
