{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build && php artisan storage:link && php artisan r2:setup && php artisan optimize"
  },
  "cron": {
    "* * * * *": {
      "command": "php artisan schedule:run"
    }
  }
}
